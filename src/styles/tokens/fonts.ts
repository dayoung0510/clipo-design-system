import { defineTokens } from '@chakra-ui/react'

export const fonts = defineTokens.fonts({
  heading: { value: 'Pretendard GOV' }, // custom
  body: { value: 'Pretendard GOV' }, // custom
  mono: { value: 'Pretendard GOV' }, // custom
})
